#!/bin/bash

# Open a list of files using xdg-open
# Author: Jason Ajmo

usage() {
	echo "Usage: open file1 [file2 file3...]"
}

if [[ $# -eq 0 ]]; then
	usage
	exit 1
fi

for file in "$@";
do
	nohup xdg-open "$file" &> /dev/null &
done

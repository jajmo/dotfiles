#!/bin/bash

# Remove orphaned packages
# Author: Jason Ajmo

# Get list of orphaned packages
orphan_packages=$(pacman -Qdtq)

# If there is at least 1, remove them
if [[ -z $orphan_packages ]]; then
	echo "Packages are clean"
	exit
else
	sudo pacman -Rsn $orphan_packages
fi
